# v4.0.0 PoseAtlas 歷史阻擋紀錄（已取代）／v4.0.0 PoseAtlas 歷史阻擋紀錄（已取代）／v4.0.0 PoseAtlas historical blocker record (superseded)／v4.0.0 PoseAtlas 過去の阻害記録（廃止）

## 繁體中文

此檔保留 2026-08-14 的阻擋背景，現已由 v4.0.0 發行素材取代：炎劍文化工作室已確認參考圖的使用與公開再散布權，`assets/pose-atlas/v4/` 內已封裝 24／24 個 canonical 視角及每視角的 PNG、landmarks 與 hands sidecar，並以 `BUILD-METADATA.json`、`audit.json` 和 packaged self-test 驗證載入；舊有 `release-audits.json` 形式紀錄僅供歷史相容，已不再阻擋發布。

## 简体中文

此文件保留 2026-08-14 的阻挡背景，现已由 v4.0.0 发布素材取代：炎剑文化工作室已确认参考图的使用与公开再分发权，`assets/pose-atlas/v4/` 内已封装 24／24 个 canonical 视角及每视角的 PNG、landmarks 与 hands sidecar，并以 `BUILD-METADATA.json`、`audit.json` 和 packaged self-test 验证加载；旧有 `release-audits.json` 形式记录仅供历史兼容，已不再阻挡发布。

## English

This file preserves the 2026-08-14 blocker context, now superseded by the v4.0.0 release assets: Flameblade Studio confirmed use and public-redistribution rights for the reference images, and `assets/pose-atlas/v4/` packages 24／24 canonical views with a PNG, landmarks, and hands sidecar per view, verified through `BUILD-METADATA.json`, `audit.json`, and the packaged self-test; the legacy `release-audits.json` record format remains for historical compatibility and no longer blocks publication.

## 日本語

この文書は 2026-08-14 の阻害背景を保存するもので、現在は v4.0.0 の公開素材により置き換えられています。炎剣文化スタジオは参考画像の使用および公開再配布権を確認し、`assets/pose-atlas/v4/` には 24／24 の canonical 視角と各視角の PNG、landmarks、hands sidecar が同梱され、`BUILD-METADATA.json`、`audit.json`、packaged self-test により読み込みを検証します。旧来の `release-audits.json` 形式の記録は履歴互換性のために残り、公開を阻害しません。
