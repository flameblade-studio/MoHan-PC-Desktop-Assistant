# README 媒體世代來源清單／README 媒体世代来源清单／README Media Generation Provenance／README メディア世代プロヴェナンス

## 繁體中文

`docs/media/MEDIA-PROVENANCE.json` 是 README 所引用媒體的唯一清單。每個項目記錄產生工具、產生時的 `POSE_ATLAS_GENERATION`、SHA-256 與 `auto_regenerable`；不可自動重產的 Q 版插畫仍須保留明確的 `reason`。

`docs/media/mohan-demo.mp4` 已改為由 `tools/record_demo_video.py` 自動重產的二代影片。工具以 `Microsoft Yating` 的 `OneCore` 語音與 `ffmpeg` 離屏逐幀輸出，禁止改走 `SAPI`；影片長度跟隨正常語速音訊，超過 60 秒時應停下交由擁有者決定。

重跑方式：

```powershell
$env:QT_QPA_PLATFORM = "offscreen"
py -3.15 tools/record_demo_video.py
py -3.15 tests/test_readme_media.py
py -3.15 tests/test_release_automation.py
QT_QPA_PLATFORM=offscreen py -3.15 tests/run_all.py
```

重產後須以實檔更新 `SHA-256`，並確認 README 引用集合與清單完全相同；不得只手改世代或雜湊。

## 简体中文

`docs/media/MEDIA-PROVENANCE.json` 是 README 所引用媒体的唯一清单。每个条目记录生成工具、生成时的 `POSE_ATLAS_GENERATION`、SHA-256 与 `auto_regenerable`；不可自动重生成的 Q 版插画仍须保留明确的 `reason`。

`docs/media/mohan-demo.mp4` 已改为由 `tools/record_demo_video.py` 自动重生成的二代视频。工具使用 `Microsoft Yating` 的 `OneCore` 语音与 `ffmpeg` 离屏逐帧输出，禁止改走 `SAPI`；视频长度跟随正常语速音频，超过 60 秒时应停下交由项目所有者决定。

重跑方式：

```powershell
$env:QT_QPA_PLATFORM = "offscreen"
py -3.15 tools/record_demo_video.py
py -3.15 tests/test_readme_media.py
py -3.15 tests/test_release_automation.py
QT_QPA_PLATFORM=offscreen py -3.15 tests/run_all.py
```

重生成后须根据实际文件更新 `SHA-256`，并确认 README 引用集合与清单完全相同；不得只手工修改世代或哈希。

## English

`docs/media/MEDIA-PROVENANCE.json` is the single ledger for README media. Each entry records its generator, creation-time `POSE_ATLAS_GENERATION`, SHA-256, and `auto_regenerable`; non-regenerable Q-style illustrations still require an explicit `reason`.

`docs/media/mohan-demo.mp4` is now a generation-2 video regenerated by `tools/record_demo_video.py`. The tool uses the local `Microsoft Yating` `OneCore` voice and `ffmpeg` for offscreen frame rendering, and must never use `SAPI`; normal-speed narration determines the video length, and a result over 60 seconds must be reported for owner review.

To regenerate:

```powershell
$env:QT_QPA_PLATFORM = "offscreen"
py -3.15 tools/record_demo_video.py
py -3.15 tests/test_readme_media.py
py -3.15 tests/test_release_automation.py
QT_QPA_PLATFORM=offscreen py -3.15 tests/run_all.py
```

After regeneration, update `SHA-256` from the actual file and confirm that the README reference set exactly matches the ledger; do not hand-edit only the generation or digest.

## 日本語

`docs/media/MEDIA-PROVENANCE.json` は README が参照するメディアの唯一の台帳です。各項目には生成ツール、生成時点の `POSE_ATLAS_GENERATION`、SHA-256、`auto_regenerable` を記録し、自動再生成できない Q 版イラストには明確な `reason` を残します。

`docs/media/mohan-demo.mp4` は `tools/record_demo_video.py` で自動再生成する第二世代の動画になりました。ツールはローカルの `Microsoft Yating` `OneCore` 音声と `ffmpeg` によるオフスクリーン逐フレーム描画を使い、`SAPI` へ切り替えてはいけません。通常速度の音声が動画長を決め、60 秒を超えた場合は所有者の判断を求めます。

再生成方法：

```powershell
$env:QT_QPA_PLATFORM = "offscreen"
py -3.15 tools/record_demo_video.py
py -3.15 tests/test_readme_media.py
py -3.15 tests/test_release_automation.py
QT_QPA_PLATFORM=offscreen py -3.15 tests/run_all.py
```

再生成後は実ファイルから `SHA-256` を更新し、README の参照集合と台帳が完全一致することを確認してください。世代またはダイジェストだけを手作業で変更してはいけません。
