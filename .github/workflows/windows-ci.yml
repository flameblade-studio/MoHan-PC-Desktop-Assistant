name: Windows CI

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: windows-ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    runs-on: windows-latest
    timeout-minutes: 30

    steps:
      - name: Check out source
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7

      - name: Set up Python
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7
        with:
          python-version: "3.14"
          cache: pip

      - name: Install dependencies
        run: python -m pip install -r requirements.txt

      - name: Compile source
        run: python -m compileall -q .

      - name: Audit public release contents
        run: python tools/audit_public_release.py

      - name: Run full regression suite
        run: python tests/run_all.py

  installer:
    name: installer
    needs: test
    runs-on: windows-latest
    timeout-minutes: 45
    steps:
      - name: Check out source
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
      - name: Set up Python
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7
        with:
          python-version: "3.14"
          cache: pip
      - name: Install runtime dependencies
        run: python -m pip install -r requirements.txt
      - name: Install Windows installer toolchains
        shell: pwsh
        run: |
          choco install innosetup --version=6.7.1 --allow-downgrade --no-progress -y
          choco install wixtoolset --version=3.14.1.20250415 --allow-downgrade --no-progress -y
      - name: Build packaged application
        shell: pwsh
        run: .\build.ps1 -Version "2.0.15-ci" -Python (Get-Command python).Source
      - name: Build EXE and MSI installers
        shell: pwsh
        run: |
          .\installer\build_installers.ps1 `
            -AppDir "dist\MoHan-Desktop-Assistant-2.0.15-ci" `
            -Version "2.0.15-ci" `
            -Tag "v2.0.15-ci" `
            -OutputDir "ci-installers"
      - name: Install, self-test, and uninstall both formats
        shell: pwsh
        run: .\installer\test_installers.ps1 -ArtifactsDir "ci-installers" -Version "2.0.15-ci"
