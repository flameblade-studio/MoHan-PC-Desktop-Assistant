### 消費端截圖基線閘門／消费端截图基线关卡／Consumer visual screenshot baseline gate／消費者向け視覚スクリーンショット・ベースラインゲート

* 新增 `tools/render_visual_baseline.py` 與 `tests/test_visual_baseline.py`，以 Qt 離屏固定 `idle_front`、發話、閉眼、頭部裁圖與可用 yaw 畫面，並以 `--check` 的 RGBA 差異像素、差異占比及連通區塊門檻攔截消費端合成漂移／新增 `tools/render_visual_baseline.py` 与 `tests/test_visual_baseline.py`，以 Qt 离屏固定 `idle_front`、发话、闭眼、头部裁图与可用 yaw 画面，并以 `--check` 的 RGBA 差异像素、差异占比及连通区块门槛拦截消费端合成漂移／Add `tools/render_visual_baseline.py` and `tests/test_visual_baseline.py` to pin `idle_front`, speech, closed-eye, head-crop and available-yaw scenes offscreen with Qt, and use `--check` RGBA pixel, ratio and connected-block thresholds to catch consumer compositor drift／`tools/render_visual_baseline.py` と `tests/test_visual_baseline.py` を追加し、Qt のオフスクリーン描画で `idle_front`、発話、閉眼、頭部クロップ、利用可能な yaw 画面を固定し、`--check` の RGBA 差分画素・割合・連結ブロック閾値で消費者向け合成のずれを検出
