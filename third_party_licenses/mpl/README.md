# MPL 原始碼與授權／MPL 原始码与授权／MPL sources and licenses／MPL ソースとライセンス

## 繁體中文

本目錄提供墨寒所使用之 MPL 元件的完整授權文字、原始通知及對應原始碼。開啟 `components.json`，依名稱與版本找到 `source_archive.path`；解開 `sources/` 中相應 ZIP 即可取得源碼，無須帳號、付款或另行請求。certifi 與 tqdm 保存實際安裝的 Python 原始檔及資料；orjson 保存經官方 PyPI SHA-256 驗證的 3.12.0 原始碼發行包內容。實際檔案雜湊與原始碼狀態逐項記錄，不宣稱已重現上游編譯。

certifi 屬於 Windows 產品相依鏈及本機美術工具；tqdm 與 orjson 屬於本機美術工具，隨附其源碼證據不代表程式會載入它們。MPL 涵蓋的檔案仍依 MPL-2.0 提供；MIT、Apache 及原作者聲明原樣保留。墨寒自身的授權不限制使用者對這些第三方源碼的既有權利。散布修改版本前須更新對應源碼、通知及雜湊。完整義務見 `MPL-2.0.txt`，其中商標授權不在授權範圍內。

## 简体中文

本目录提供墨寒所使用的 MPL 组件的完整授权文字、原始通知及对应源代码。打开 `components.json`，按名称和版本找到 `source_archive.path`；解开 `sources/` 中对应 ZIP 即可取得源代码，无须账号、付款或另行请求。certifi 与 tqdm 保存实际安装的 Python 原始文件及数据；orjson 保存经官方 PyPI SHA-256 验证的 3.12.0 源代码发行包内容。实际文件哈希与源代码状态逐项记录，不声称已重现上游编译。

certifi 属于 Windows 产品依赖链及本地美术工具；tqdm 与 orjson 属于本地美术工具，附带其源代码证据不代表程序会加载它们。MPL 涵盖的文件仍按 MPL-2.0 提供；MIT、Apache 及原作者声明原样保留。墨寒自身的授权不限制用户对这些第三方源代码的既有权利。分发修改版本前须更新对应源代码、通知及哈希。完整义务见 `MPL-2.0.txt`，其中商标授权不在授权范围内。

## English

This directory supplies the complete MPL text, original notices and corresponding sources of the MPL components used by MoHan. Open `components.json`, locate `source_archive.path` by component name and version, and unpack the matching ZIP under `sources/`; no account, payment or separate request is necessary. certifi and tqdm preserve the installed Python sources and data. orjson preserves the contents of its official 3.12.0 source distribution, verified against the PyPI SHA-256. File hashes and source status are recorded individually; an upstream reproducible build is not claimed.

certifi belongs to the Windows product dependency chain and local art tooling; tqdm and orjson belong to local art tooling. Shipping their source evidence does not imply loading them at runtime. MPL-covered files remain available under MPL-2.0; MIT, Apache and original author notices are preserved. MoHan's own license does not restrict recipients' existing rights to these third-party sources. Update corresponding sources, notices and hashes before distributing modified versions. Full obligations appear in `MPL-2.0.txt`; trademark rights are not granted.

## 日本語

このディレクトリは、墨寒が使用する MPL コンポーネントのライセンス全文、元の通知および対応するソースコードを提供します。`components.json` で名前とバージョンから `source_archive.path` を確認し、`sources/` 内の対応する ZIP を展開してください。アカウント、支払い、別途の請求は不要です。certifi と tqdm はインストールされた Python ソースとデータを保存しています。orjson は公式 PyPI SHA-256 で検証した 3.12.0 ソース配布物の内容を保存しています。ファイルのハッシュとソースの状態を個別に記録し、上流のビルド再現を主張しません。

certifi は Windows 製品の依存関係とローカル美術ツールに属し、tqdm と orjson はローカル美術ツールに属します。ソース証拠の同梱は、実行時の読み込みを意味しません。MPL 対象ファイルは MPL-2.0 で提供し、MIT、Apache および元の著作者の通知を保持します。墨寒自身のライセンスは、第三者ソースに対する受領者の既存の権利を制限しません。変更版の配布前には対応ソース、通知、ハッシュを更新してください。全義務は `MPL-2.0.txt` に記載され、商標権は許諾されません。
