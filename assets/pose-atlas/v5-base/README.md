# 原生來源中繼資料重建／原生来源元数据重建／PoseAtlas exact-source metadata rebuild／原生ソースのメタデータ再構築

## 繁體中文

此目錄是 24 視角開發工作證據。PNG 逐位元複製核定 v5-base source，尺寸、RGB／alpha 完整保留。landmarks sidecar 同時保存 alpha body registration 與同源 478 點 face evidence；手部使用既有本機 ONNX。各視角只記錄同源單臉結果，待補項目逐一列明。正式發布與完整外觀驗收仍待完成。

## 简体中文

此目录是 24 视角开发工作证据。PNG 逐位复制核定 v5-base source，尺寸、RGB／alpha 完整保留。landmarks sidecar 同时保存 alpha body registration 与同源 478 点 face evidence；手部使用既有本机 ONNX。各视角只记录同源单脸结果，待补项目逐一列明。正式发布与完整外观验收仍待完成。

## English

This is an isolated 24-view development evidence tree. Each PNG is byte-exact to the approved v5-base source; dimensions, RGB, and alpha remain exact. Landmark sidecars retain alpha body registration and same-source 478-point face evidence; hands use the existing bundled ONNX pipeline. Each view records only same-source single-face evidence, with outstanding results listed explicitly. Release and complete appearance acceptance remain pending.

## 日本語

これは24視点の隔離された開発証拠ツリーです。各PNGは承認済みv5-baseソースとバイト単位で一致し、サイズ、RGB／アルファをそのまま保持します。landmarks sidecarにはアルファによる身体登録と同一ソースの478点顔証拠を保存し、手部には既存の同梱ONNX処理を使用します。各視点は同一ソースの単一顔の証拠だけを記録し、追加が必要な結果を明示します。リリースと外観全体の承認は準備中です。
