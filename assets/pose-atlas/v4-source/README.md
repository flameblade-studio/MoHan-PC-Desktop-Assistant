# PoseAtlas v4 候選素材說明／PoseAtlas v4 候选素材说明／PoseAtlas v4 Candidate Assets／PoseAtlas v4 候補素材

## 繁體中文

這個目錄只保存 v4.0.0 PoseAtlas 的內部候選圖，不是正式發布資產。候選圖涵蓋 `yaw-180`、`yaw-165`、`yaw-150`、`yaw-135`、`yaw-120`、`yaw-105`、`yaw-090`、`yaw-075`、`yaw-060`、`yaw-045`、`yaw-030`、`yaw-015`、`yaw+000`、`yaw+015`、`yaw+030`、`yaw+045`、`yaw+060`、`yaw+075`、`yaw+090`、`yaw+105`、`yaw+120`、`yaw+135`、`yaw+150`、`yaw+165`，全部使用 `pitch+00`。

這些 PNG 是依照使用者提供的參考圖產生的內部候選。產生來源、素材授權與再散布權仍須逐項確認，完成前不得視為正式發布資產。

目前已完成的離線檔案檢查只有：24 張 PNG 均為 1024×1536、RGBA、具有有效透明邊界，且主體未貼齊畫布邊緣。另已建立 `candidate-evidence/hand-detection-report.json`，記錄 19 組真實模型觀測、399 個 21 點座標；這仍不是完整正式 hands sidecar。來源與授權紀錄位於 `PROVENANCE.md` 與 `PROVENANCE.json`。這些結果不代表來源權利、人物身分一致性、服裝與配件轉面一致性、身體 landmarks 或正式稽核已完成。

候選圖在來源與再散布權、人物與服裝逐視角審查、landmarks、hands sidecar、雜湊、載入與正式 `release-audits.json` 完成前，不得複製到 `assets/pose-atlas/v4/`，也不得解除 v4.0.0 發布阻擋。

## 简体中文

此目录只保存 v4.0.0 PoseAtlas 的内部候选图，不是正式发布资产。候选图覆盖 `yaw-180`、`yaw-165`、`yaw-150`、`yaw-135`、`yaw-120`、`yaw-105`、`yaw-090`、`yaw-075`、`yaw-060`、`yaw-045`、`yaw-030`、`yaw-015`、`yaw+000`、`yaw+015`、`yaw+030`、`yaw+045`、`yaw+060`、`yaw+075`、`yaw+090`、`yaw+105`、`yaw+120`、`yaw+135`、`yaw+150`、`yaw+165`，全部使用 `pitch+00`。

这些 PNG 是依据用户提供的参考图生成的内部候选。生成来源、素材许可与再分发权仍须逐项确认，完成前不得视为正式发布资产。

目前完成的离线文件检查只有：24 张 PNG 均为 1024×1536、RGBA、具有有效透明边界，并且主体未贴齐画布边缘。另已建立 `candidate-evidence/hand-detection-report.json`，记录 19 组真实模型观测、399 个 21 点坐标；这仍不是完整正式 hands sidecar。来源与授权记录位于 `PROVENANCE.md` 与 `PROVENANCE.json`。这些结果不代表来源权利、人物身份一致性、服装与配件转面一致性、身体 landmarks 或正式审计已经完成。

在来源与再分发权、人物与服装逐视角审查、landmarks、hands sidecar、哈希、加载与正式 `release-audits.json` 完成前，不得把候选图复制到 `assets/pose-atlas/v4/`，也不得解除 v4.0.0 发布阻挡。

## English

This directory contains internal v4.0.0 PoseAtlas candidates only. It is not a release asset directory. The candidates cover `yaw-180`, `yaw-165`, `yaw-150`, `yaw-135`, `yaw-120`, `yaw-105`, `yaw-090`, `yaw-075`, `yaw-060`, `yaw-045`, `yaw-030`, `yaw-015`, `yaw+000`, `yaw+015`, `yaw+030`, `yaw+045`, `yaw+060`, `yaw+075`, `yaw+090`, `yaw+105`, `yaw+120`, `yaw+135`, `yaw+150`, and `yaw+165`, all at `pitch+00`.

These PNG files are internal candidates generated from references supplied by the user. Generation provenance, asset licensing, and redistribution rights must be confirmed per asset before they can be treated as release assets.

The completed offline file check covers only these facts: all 24 PNG files are 1024×1536 RGBA images with valid non-empty alpha bounds, and the subject is not flush with the canvas edge. `candidate-evidence/hand-detection-report.json` now records nineteen real model observations and 399 total 21-point coordinates, but it is not a complete formal hands-sidecar set. The source and authorization record is in `PROVENANCE.md` and `PROVENANCE.json`. None of this proves provenance rights, identity continuity, clothing and accessory turn consistency, body landmarks, or formal audit completion.

The candidates must not be copied into `assets/pose-atlas/v4/` or used to remove the v4.0.0 release blocker until provenance and redistribution rights, per-view character and clothing review, landmarks, hands sidecars, hashes, loading evidence, and genuine `release-audits.json` are complete.

## 日本語

このディレクトリには v4.0.0 PoseAtlas の内部候補画像だけを保存します。公開用素材ディレクトリではありません。候補は `yaw-180`、`yaw-165`、`yaw-150`、`yaw-135`、`yaw-120`、`yaw-105`、`yaw-090`、`yaw-075`、`yaw-060`、`yaw-045`、`yaw-030`、`yaw-015`、`yaw+000`、`yaw+015`、`yaw+030`、`yaw+045`、`yaw+060`、`yaw+075`、`yaw+090`、`yaw+105`、`yaw+120`、`yaw+135`、`yaw+150`、`yaw+165` を含み、すべて `pitch+00` です。

これらの PNG は、ユーザーが提供した参考画像を基に生成した内部候補です。生成出典、素材ライセンス、再配布権は各素材ごとに確認する必要があり、完了前は公開用素材として扱いません。

完了したオフラインファイル検査は、24 枚すべてが 1024×1536、RGBA、空でない透明境界を持ち、主体が canvas の端に接していないことだけを確認します。`candidate-evidence/hand-detection-report.json` には実モデルによる 19 組の観測と合計 399 個の 21 点座標を記録しましたが、正式な hands sidecar 一式ではありません。出典と許諾の記録は `PROVENANCE.md` と `PROVENANCE.json` にあります。これらの結果は、出典権利、人物同一性、衣装と装飾品の回転整合性、body landmarks、正式監査の完了を証明しません。

出典と再配布権、各視角の人物と衣装の審査、landmarks、hands sidecar、hash、load evidence、真正な `release-audits.json` が揃うまで、候補を `assets/pose-atlas/v4/` にコピーしたり、v4.0.0 の公開阻害を解除したりしてはいけません。
