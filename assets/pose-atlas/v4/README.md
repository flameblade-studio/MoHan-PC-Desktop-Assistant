# PoseAtlas v4 發布素材／PoseAtlas v4 发布素材／PoseAtlas v4 release assets／PoseAtlas v4 公開素材

## 繁體中文

`assets/pose-atlas/v4/` 收錄隨墨寒 v4.0.0 四平台封裝的 24 視角、landmarks／hands；`BUILD-METADATA.json` 保留來源與正規化資料，`audit.json` 記錄 24／24 的實際資產檢查。

## 简体中文

`assets/pose-atlas/v4/` 收录随墨寒 v4.0.0 四平台打包的 24 个视角、landmarks／hands；`BUILD-METADATA.json` 保留来源与归一化资料，`audit.json` 记录 24／24 的实际资产检查。

## English

`assets/pose-atlas/v4/` contains the 24 views and landmarks／hands packaged for MoHan v4.0.0 on four platforms; `BUILD-METADATA.json` retains source and normalization data, and `audit.json` records the actual 24／24 asset check.

## 日本語

`assets/pose-atlas/v4/` には墨寒 v4.0.0 の四プラットフォーム向けに封入する 24 視角と landmarks／hands を収録し、`BUILD-METADATA.json` は出典と正規化情報、`audit.json` は実際の 24／24 資産検査を記録します。
