"""二代素體產線的量測門檻，連同各自的校準範圍與已知盲點。

集中在一處的理由：這些數字大半跨檔共用（剪影切割、背景色距、角度帶），
分散在各檔案會各自漂移，而且校準依據會散失。每個常數旁邊記的不是「這是什麼」，
是**它憑什麼是這個值、在哪個範圍內有效、以及它分不出什麼**——
這三件事才是本輪反覆踩坑換來的東西。

範圍外一律不適用。本輪已三次把某範圍內校準的門檻套到範圍外：
單一視角的強度曲線當通則、眼距比套到側面帶、服裝門檻套到後四分之三。
"""

# ── 影像切割 ─────────────────────────────────────────────────────────
# 控制圖 silhouette 是 0/255 的二值圖，24 只是避開 JPEG 邊緣雜訊的下限。
SILHOUETTE_ON = 24
# 最近鄰縮放後仍是二值，127 取中點。
BINARY_MIDPOINT = 127
# 前景與背景的 RGB 色距總和門檻。模型畫出的背景與填入的底板相差約 21~34，
# 人物與背景相差遠大於 40，兩者之間有餘裕。
BACKGROUND_DISTANCE = 40
# person_count 專用，比 BACKGROUND_DISTANCE 略低以保住膚色頸部——
# 用灰階＋門檻 18 會把亮度 205 的頸部判成背景（背景 194），
# 於是一個人被切成頭與身體兩塊、數成兩人。
FIGURE_DISTANCE = 26
# 連通分量要多大才算一個人。
FIGURE_MIN_AREA_RATIO = 0.015

# ── 角度帶（formal yaw 的絕對值，單位度）──────────────────────────────
# 帶的邊界對應提示詞的方位措辭，也對應各檢查的有效範圍。
FRONT_MAX = 22          # 正面：明白斷言胸與肚臍朝向鏡頭
THREE_QUARTER_MIN = 23
THREE_QUARTER_MAX = 67  # 四分之三：仍須明寫正面可見，否則背面四分之三也滿足措辭
PROFILE_MAX = 112       # 純側面
REAR_QUARTER_MAX = 157  # 後四分之三
REAR_NEGATIVE_MAX = 113 # 小於此值的視角要在負向詞排除背面
BACK_MIN = 150          # 背面

# ── 眼距佔臉寬（分辨臉部朝向）──────────────────────────────────────
# YuNet 的信心分數在正反之間完全不可分（正面 0.932、背面 0.891），
# 臉部面積也重疊。只有眼距佔臉寬分得開。
# 樣本：正面 0.458/0.462/0.495/0.511；轉身仍朝前 0.203/0.303/0.337/0.401；
#       各種背面 0.034/0.043/0.064/0.077/0.081/0.141。
EYE_SPAN_FRONT_MIN = 0.35   # 正面帶：低於此值疑似正反顛倒
EYE_SPAN_TURNED_MIN = 0.20  # 四分之三帶：低於此值疑似轉成背面
EYE_SPAN_REAR_MAX = 0.20    # 後四分之三與背面：高於此值疑似朝向鏡頭
# **68–112 度不適用**：純側面只看得到一隻眼，眼距必然極小，
# 一張乾淨的 90 度側面只有 0.064。該帶的方位由角度指標負責。

# ── 服裝 ────────────────────────────────────────────────────────────
# 膚色偏暖（R 明顯大於 B），泳裝是淺灰藍（R 約等於或小於 B）。
SKIN_WARM_MARGIN = 4        # R-B 小於等於此值視為布料
CHEST_BAND = (0.26, 0.40)   # 佔身高的比例
# 有上衣 0.121~0.344、無上衣 0.046~0.059，0.09 落在間隙中央。
# 正樣本 4 個、負樣本 2 個——是絆線不是證明。
CHEST_GARMENT_MIN = 0.09
# **有效範圍只到 90 度**：105 度時上衣明明在身上卻只有 0.085，胸前已轉離鏡頭。
GARMENT_CHECK_MAX_YAW = 90

# ── 幾何 ────────────────────────────────────────────────────────────
# 下半身起點佔身高的比例。手臂搆不到這裡，是唯一不受手臂姿勢污染的角度訊號。
LOWER_BODY_START = 0.52
# 頸線佔身高的比例。控制剪影是光頭的，輸出長出頭髮會讓全身 IoU 無條件下降。
NECK_LINE = 0.13
# 純文生圖基準 0.364；低於 0.50 代表幾何完全沒進去。
# 注意這是「有沒有進去」的門檻，**不能用來判角度**——一張正確的 45 度圖只有
# 0.528，而斜角的 IoU 本來就比正面低。判角度要看峰值落在哪裡。
GEOMETRY_IOU_MIN = 0.50
# 角度判定容忍前兩名：A-pose 的正反剪影完全相同，+045 與 +135 的 IoU 一模一樣。
ANGLE_RANK_TOLERANCE = 2

# ── 量測工具的自我驗證 ──────────────────────────────────────────────
# 對照 candidate3 官方斷面，實測差 0.000 cm。
SECTION_SELF_CHECK_TOLERANCE_CM = 0.05
# 四肢形變後軀幹斷面不得改變；實測最大偏差 0.0012 cm。
TORSO_PRESERVE_TOLERANCE_CM = 0.01
# 渲染器對照官方控制圖：剪影 IoU 1.0000、法線平均餘弦 +0.9999。
RENDER_IOU_MIN = 0.995
RENDER_NORMAL_COSINE_MIN = 0.98

# ── 幾何運算 ────────────────────────────────────────────────────────
TRIANGLE_EDGES = 3
SEGMENT_ENDPOINTS = 2       # 一條線段兩個端點；封閉環每個節點度數也是 2
DEGENERATE_AREA = 1e-12     # 退化三角形
DEGENERATE_AREA_LOOSE = 1e-9

# ── 量產 ────────────────────────────────────────────────────────────
# 容許一個視角偶發失敗；累計兩個代表系統性問題，停下來比跑完四小時划算。
MAX_FAILED_VIEWS = 2


# ── 早期版本（v9/v10）沿用的門檻 ────────────────────────────────────
BACK_YAW = 180              # 正規形式是 -180，既有 17 張用 +180，統一成 +180
BACK_PROMPT_MIN = 120       # v9 從此角度起改用背面專用的負向詞
GRAY_FIGURE_DISTANCE = 18   # v10 的灰階切割門檻。**這個值會把亮度 205 的膚色
                            # 頸部判成背景（背景 194），一個人被切成兩塊、
                            # 數成兩人。v11 起改用 RGB 色距的 FIGURE_DISTANCE。
REAR_FACE_FORBIDDEN = 135   # v10：此角度以上不該偵測到臉
FRONT_FACE_REQUIRED = 30    # v10：此角度以下必須偵測到臉
FACE_AREA_MIN = 0.004       # 臉部佔畫面的下限。**臉部面積分不出正反**——
                            # 正面 0.0109、背面 0.0061~0.0069 有重疊，
                            # 正反要看眼距比。

# ── 探針專用 ────────────────────────────────────────────────────────
PLATE_DISTANCE = 45         # 與已知底板色的距離。實測模型畫的背景與底板差
                            # 21~34，餘裕只有 11，偏薄；改用角落中位色較穩。
SHOULDER_BAND_CM = 1.5      # 肩關節高度上下取樣的帶寬
LOWER_BODY_GOOD = 0.70      # 下半身 IoU 高於此值視為幾何守住
FOREGROUND_FRAC_MIN = 0.05  # 前景佔比的合理區間；超出代表切法把背景算進去了
FOREGROUND_FRAC_MAX = 0.35
IOU_GAIN_MIN = 0.05         # 相對純 t2i 基準要有這麼多增益才算幾何有進去
CHROMA_GAIN = 1.35          # 前景彩度要高過灰模自身這麼多倍才算完成上色
PROFILE_YAW = 90
MIN_TRACKING_SAMPLES = 4    # 角度追蹤判定所需的最少可測樣本
MONOTONIC_MIN = 0.75        # 相鄰差值同號的比例下限
LARGEST_GAP_MAX = 0.35      # 最大間隙佔全距的上限；超過代表呈階梯狀分群
