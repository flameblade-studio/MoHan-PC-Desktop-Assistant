# =================================================================
# MoHan Project - Roo Code Token Optimization & Safety Rules
# =================================================================

# 1. 重點排除：嚴禁進入以下資料夾以節省 Token (關鍵省錢區)
# -----------------------------------------------------------------
# 排除 Python 虛擬環境與套件 (這是最大的 Token 消耗源)
/venv/
/.venv/
/env/
/node_modules/
/__pycache__/
*.pyc

# 2. 排除建置產物與編譯暫存 (Rust & Python Build Artifacts)
# -----------------------------------------------------------------
# 排除 Rust 編譯資料夾 (target 資料夾通常巨大，絕不讀取)
/target/
# 排除 Python 封裝與分發產物
/dist/
/build/
/*.egg-info/
/*.whl

# 3. 排除版本控制與開發工具隱藏檔
# -----------------------------------------------------------------
/.git/
/.github/
/.vscode/
/.idea/
/.ruff_cache/
/.pytest_cache/
/.mypy_cache/

# 4. 排除大型資產與媒體文件 (墨寒的換裝與渲染資源)
# -----------------------------------------------------------------
# 這些檔案 Roo Code 看不懂，讀取只會浪費錢
*.png
*.jpg
*.jpeg
*.gif
*.ico
*.mp3
*.wav
*.mp4
*.onnx
*.bin
*.pt
*.weights

# 5. 指令行為約束 (Core Instructions for Roo Code)
# -----------------------------------------------------------------
# - 絕不主動讀取上述排除清單中的任何檔案。
# - 在進行全域搜尋 (grep) 時，必須跳過上述資料夾。
# - 優先使用 `list_files` 觀察結構，而非直接讀取長度超過 500 行的檔案。
# - 針對 Rust (PyO3) 部分，僅讀取 .rs 原始碼，不讀取編譯後的 .pyd 或 .so 檔。
# - 始終遵循「Python 之禪 (The Zen of Python)」：簡潔優於複雜。
# - 維持 Python 3.15rc1 的最新特性（如 Lazy Imports, JIT 友善語法）。

# 6. 專案特化規則 (MoHan Specific)
# -----------------------------------------------------------------
# - 保持 18 個模組之間的低耦合 (Low Coupling) 關係。
# - 若需修改核心邏輯，請優先檢查稽核工具與回歸測試腳本。
# - 在執行終端指令前，務必確認當前 Python 環境版本為 3.15rc1。