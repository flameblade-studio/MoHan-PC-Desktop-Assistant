# =================================================================
# MoHan Project - Roo Code Token Optimization & Safety Rules
# =================================================================

# 1. 搜尋範圍：聚焦清單以外的專案來源以節省 Token
# -----------------------------------------------------------------
# 搜尋時略過 Python 虛擬環境與套件（最大的 Token 消耗源）
/venv/
/.venv/
/env/
/node_modules/
/__pycache__/
*.pyc

# 2. 搜尋時略過建置產物與編譯暫存 (Rust & Python Build Artifacts)
# -----------------------------------------------------------------
# Rust 原始碼位於 target 以外；target 僅存放大型編譯產物
/target/
# Python 原始碼位於下列封裝與分發產物以外
/dist/
/build/
/*.egg-info/
/*.whl

# 3. 搜尋時略過版本控制與開發工具隱藏檔
# -----------------------------------------------------------------
/.git/
/.github/
/.vscode/
/.idea/
/.ruff_cache/
/.pytest_cache/
/.mypy_cache/

# 4. 一般程式搜尋聚焦於大型資產與媒體文件以外的文字來源
# -----------------------------------------------------------------
# 這些格式交由對應的媒體檢視工具處理，Roo Code 保留 Token 給文字來源
*.png
*.jpg
*.jpeg
*.gif
*.ico
*.mp3
*.wav
*.mp4
*.onnx
*.bin
*.pt
*.weights

# 5. 指令行為約束 (Core Instructions for Roo Code)
# -----------------------------------------------------------------
# - 主動讀取範圍採用上述清單以外的文字來源。
# - 全域搜尋 (grep) 明確略過上述資料夾。
# - 優先使用 `list_files` 觀察結構；超過 500 行的檔案採分段讀取。
# - Rust (PyO3) 部分聚焦於 .rs 原始碼；.pyd 與 .so 交由二進位檢查工具處理。
# - 始終遵循「Python 之禪 (The Zen of Python)」：簡潔優於複雜。
# - 維持 Python 3.15rc1 的最新特性（如 Lazy Imports, JIT 友善語法）。

# 6. 專案特化規則 (MoHan Specific)
# -----------------------------------------------------------------
# - 保持 18 個模組之間的低耦合 (Low Coupling) 關係。
# - 若需修改核心邏輯，請優先檢查稽核工具與回歸測試腳本。
# - 在執行終端指令前，務必確認當前 Python 環境版本為 3.15rc1。
